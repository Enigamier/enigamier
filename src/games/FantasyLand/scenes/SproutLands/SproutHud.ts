import type { BaseSceneContext } from '@/scenes/BaseScene'
import { HudScene } from '@/scenes/HudScene'

export class SproutHudScene extends HudScene {
  public id = 'SproutHud'

  public load(context: BaseSceneContext): void {

    super.load(context)
  }
}
